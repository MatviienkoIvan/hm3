import { AlphabeticPipePipe } from './alphabetic-pipe.pipe';

describe('AlphabeticPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabeticPipePipe();
    expect(pipe).toBeTruthy();
  });
});
